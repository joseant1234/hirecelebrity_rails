%tr{id: "#{dom_id service}", class: "#{color_tr(service)}"}
  %td= service.name
  %td
    .two-lines= service.categories.sort_by(&:name).map(&:name).join(', ').html_safe
  %td= service.featured.humanize
  %td
    = link_to admin_service_path(service), class: 'black-text' do
      %i.material-icons visibility
    %span &nbsp;
    = link_to edit_admin_service_path(service), class: 'black-text' do
      %i.material-icons edit
    %span &nbsp;
    = link_status service
